from app.models import (
    storage_models,
    setting_models,
    camera_models,
    record_models,
    permission_models,
    role_models,
    token_models,
    user_models,
    association_tables,
)
